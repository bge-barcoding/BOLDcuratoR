# Tests for specimen handling utilities
# tests/testthat/test-specimen_utils.R
#
# The standalone functions (calculate_specimen_score, determine_specimen_rank,
# apply_specimen_ranking) were refactored into the SpecimenScorer R6 class.
# Their tests live in test-specimen_processing.R now.

library(testthat)

test_that("placeholder: specimen utils now tested via specimen_processing pipeline", {
  expect_true(TRUE)
})
